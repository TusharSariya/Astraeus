"""Things this deployment computes, and the registry that admits them.

``methods`` and the artifacts it derives are presentation-support material:
computed here from published artifacts, disclosing the derivation and the
exact revisions it came from, class ``generated_display``, never on a data
path.

``registry`` is not that. Since the six evidence classes were accepted
(``docs/adr/0001-five-evidence-classes.md``, 2026-09-02) a value this
deployment computes from retrieved inputs by an owner-approved, cited method
is class ``derived_here`` and IS evidence, admitted on every data path under
the conditions that module enforces. The two live side by side here because
they answer the same question - what did this deployment compute - and the
class, not the package, decides where the answer may go.
"""
