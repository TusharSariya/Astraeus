# Tasks

- [x] Catalogue separate OSTIA and OISST source and field mappings with
      `operational: false`; keep capture adapters isolated and unregistered.
- [x] Implement bounded anonymous discovery, fetch, crop, normalization,
      uncertainty and native-mask handling.
- [x] Add representative adapter and negative-path tests.
- [x] Record live capture receipts and actual Astraeus HTTP readback, including
      the injected replay clocks and the actual-clock unavailable result.
- [x] Run the repository test, registry and specification gates; the selected
      change and specctl pass, while strict all-OpenSpec retains two unrelated
      pre-existing failures.
