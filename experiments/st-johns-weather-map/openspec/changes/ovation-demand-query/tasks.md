# Tasks

- [x] Verify the current public SWPC endpoint and retain no production body in Git.
- [x] Preserve existing normalized crop, native zero/missing-cell, percent,
  Observation-Time, Forecast-Time, and model-disclosure rules.
- [x] Implement one canonical 1 MiB request, isolated structural decode,
  finite retained metadata/cell cache, final-byte freshness, coalescing, and
  expired-refresh fail-closed behavior.
- [x] Route the existing raster and layer listing to the demand-query seam with
  receipt provenance and no storage fallback.
- [x] Add deterministic worker, cache, expiry, route, rendering, zero/missing,
  and no-fetch layer-list regressions.
- [x] Run focused gates, root specification validation, independent review, and
  browser/UI readback before merge.
