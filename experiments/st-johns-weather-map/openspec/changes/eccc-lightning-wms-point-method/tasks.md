# Tasks

- [ ] Obtain explicit owner acceptance of the source-specific WMS pixel method.
- [ ] After acceptance, map `eccc-lightning` to the exact-frame GeoMet demand path and `available-not-stored` field declarations.
- [ ] Preserve provider-returned numeric feature geometry and enforce the accepted latitude-corrected distance ceiling.
- [ ] For the accepted native bare `{}` response, publish only `lightning_observed = 0`, keep density absent, and disclose unknown sampled coordinates and distance without echoing the requested point.
- [ ] Add fixed-frame, fixed-clock worker/cache/public API verification and an actual bounded-child Linux proof.
