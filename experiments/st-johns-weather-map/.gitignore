.env
.DS_Store


# Compiled config artifacts; vite.config.ts is the source of truth and a stale
# .js shadows it silently.
vite.config.js
vite.config.d.ts

# Local copy of provider keys; never committed.
.env.keys-backup

# Pinned JPL ephemeris kernel: fetched out of band, verified by checksum.
data/ephemeris/
