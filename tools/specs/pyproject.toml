[project]
name = "astraeus-spec-tools"
version = "0.1.0"
description = "Specification governance tooling for Astraeus"
requires-python = ">=3.12,<3.15"
dependencies = [
  "jsonschema>=4.23,<5",
  "openapi-spec-validator>=0.7.2,<0.8",
  "pyyaml>=6.0.2,<7",
]

[tool.uv]
package = false
